#define _USE_MATH_DEFINES 
#define _CRT_SECURE_NO_DEPRECATE
#define ROW 9
#define COL 9
#include <stdio.h>
#include <conio.h> 
#include <math.h> 
#include<locale.h>

//zadanie 1
/*
void main() {
	int row, col;
	printf("\n");
	for (row = 1; row <= ROW; ++row)
	{
		for (col = 1; col <= COL; col++)
		{
			if (row < col)
				break;
			printf("%5d", col * row);
		}
		printf("\n");

	}

	printf("\n");
	int k = 1;
	for (row = 1; row <= 5; ++row)
	{
		for (col = 1; col <= 5; col++)
		{
			if (row <= col)
				printf("%5d ", k);
		}
			printf("\n");
	}

}	

*/